default:
    suites:
        ui_managing_books:
            contexts:
                - sylius.behat.context.setup.admin_security

                - app.tests.behat.context.book_context
            filters:
                tags: "@managing_books && @ui"
